version: 2
semantic_models:
  - name: supply
    model: ref('stg_supplies')
    label: Supply
    description: |-
      None
    defaults:
      agg_time_dimension:
    entities:
      - name: supply
        type: primary
        expr: supply_id
